# Task PRD-0101: Normalize Metadata And Package Boundary

> PRD: PRD-01 | Depends: None  
> Files: 4 | Lines: ~180 | Time: ~60min

## What to Build

Create the canonical SLAM-COKO package boundary and correct all project metadata that still points at the placeholder `AMATERASU` scaffold.

## Files

### Create: `src/anima_slam_coko/__init__.py`
```python
from .version import __version__
```

### Create: `src/anima_slam_coko/version.py`
```python
__version__ = "0.1.0"
```

### Update: `pyproject.toml`
```toml
[project]
name = "anima-slam-coko"
```

### Update: `configs/default.toml`
```toml
[project]
codename = "SLAM-COKO"
paper_arxiv = "2604.00804"
```

## Verify

```bash
uv run python -c "import anima_slam_coko; print(anima_slam_coko.__version__)"
```

## Done When

- [ ] `anima_slam_coko` imports cleanly
- [ ] paper ID and codename are corrected
- [ ] no new placeholder names remain in project metadata
