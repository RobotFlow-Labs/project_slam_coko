# Task PRD-0401: Build Replica Evaluation Runner

> PRD: PRD-04 | Depends: PRD-0303  
> Files: 3 | Lines: ~180 | Time: ~60min

## What to Build

Implement scene-by-scene training-view evaluation on Replica and emit a machine-readable summary aligned with Table 1.

## Files

### Create: `src/anima_slam_coko/eval/replica.py`
```python
def evaluate_replica_run(run_dir: Path) -> dict:
    ...
```

### Create: `tests/eval/test_replica_metrics.py`
```python
def test_replica_metrics_have_expected_keys():
    ...
```

## Verify

```bash
uv run pytest tests/eval/test_replica_metrics.py -v
```

## Done When

- [ ] PSNR, SSIM, LPIPS, depth-L1 are emitted
- [ ] output layout matches paper scene names
- [ ] values are serializable to JSON
