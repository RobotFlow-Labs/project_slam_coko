# Task PRD-0602: Integrate ROS2 Bridge Node With Runtime

> PRD: PRD-06 | Depends: PRD-0601  
> Files: 2 | Lines: ~200 | Time: ~75min

## What to Build

Wrap the offline runtime in a ROS2 node that accepts synchronized RGB-D input and emits submap / pose status updates.

## Files

### Create: `src/anima_slam_coko/ros2/bridge_node.py`
```python
class SlamCokoBridge(Node):
    def on_rgbd(self, rgb_msg, depth_msg, info_msg):
        ...
```

## Verify

```bash
uv run pytest tests/ros2/test_message_adapters.py -v
```

## Done When

- [ ] bridge can instantiate without live hardware
- [ ] runtime step path is callable from callback
- [ ] status publication points are defined
