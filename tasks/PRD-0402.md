# Task PRD-0402: Build Aria Evaluation Runner

> PRD: PRD-04 | Depends: PRD-0401  
> Files: 2 | Lines: ~160 | Time: ~60min

## What to Build

Implement room-level novel-view evaluation for Aria and preserve the paper’s split between novel-view and training-view reporting.

## Files

### Create: `src/anima_slam_coko/eval/aria.py`
```python
def evaluate_aria_run(run_dir: Path) -> dict:
    ...
```

## Verify

```bash
uv run pytest tests/eval/test_replica_metrics.py -v
```

## Done When

- [ ] novel-view and training-view sections are separated
- [ ] room identifiers `room0` and `room1` are preserved
- [ ] metrics JSON contains both modes
