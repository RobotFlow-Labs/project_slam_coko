# Task PRD-0503: Package CUDA Docker Runtime And Smoke Tests

> PRD: PRD-05 | Depends: PRD-0502  
> Files: 3 | Lines: ~160 | Time: ~60min

## What to Build

Package the API and runtime in a CUDA-capable container with dataset and artifact mounts plus a compose-level smoke test.

## Files

### Create: `Dockerfile`
```dockerfile
FROM nvidia/cuda:12.4.1-cudnn-devel-ubuntu22.04
```

### Create: `docker-compose.yml`
```yaml
services:
  slam-coko:
    build: .
```

### Create: `.env.example`
```bash
DATASET_ROOT=/Volumes/AIFlowDev/RobotFlowLabs/datasets
```

## Verify

```bash
docker compose config
```

## Done When

- [ ] compose renders without errors
- [ ] container receives dataset and artifact mounts
- [ ] runtime mode can be set via env vars
