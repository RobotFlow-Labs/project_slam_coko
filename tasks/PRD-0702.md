# Task PRD-0702: Implement Release Gates

> PRD: PRD-07 | Depends: PRD-0701  
> Files: 3 | Lines: ~180 | Time: ~75min

## What to Build

Turn paper fidelity and communication efficiency into release checks that fail when performance drifts beyond tolerance.

## Files

### Create: `src/anima_slam_coko/ops/release_checks.py`
```python
def validate_release(metrics: dict, bandwidth: dict) -> list[str]:
    ...
```

### Create: `tests/production/test_release_checks.py`
```python
def test_release_rejects_low_reduction_ratio():
    ...
```

## Verify

```bash
uv run pytest tests/production/test_release_checks.py -v
```

## Done When

- [ ] paper PSNR guardrails are enforced
- [ ] communication reduction guardrail is enforced
- [ ] failures are returned as actionable messages
