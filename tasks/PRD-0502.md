# Task PRD-0502: Add Job Execution And Artifact Endpoints

> PRD: PRD-05 | Depends: PRD-0501  
> Files: 3 | Lines: ~220 | Time: ~75min

## What to Build

Add background job execution, run status inspection, and artifact listing endpoints backed by the offline CLI pipeline.

## Files

### Create: `src/anima_slam_coko/api/jobs.py`
```python
def enqueue(req: RunRequest) -> str:
    ...
```

### Update: `src/anima_slam_coko/api/app.py`
```python
@app.get("/runs/{job_id}")
def get_run(job_id: str):
    ...
```

### Create: `tests/api/test_app.py`
```python
def test_create_and_query_run():
    ...
```

## Verify

```bash
uv run pytest tests/api/test_app.py -v
```

## Done When

- [ ] job state persists
- [ ] artifacts are discoverable by API
- [ ] missing job ids return clean errors
