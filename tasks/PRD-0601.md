# Task PRD-0601: Implement ROS2 Message Adapters

> PRD: PRD-06 | Depends: PRD-0503  
> Files: 2 | Lines: ~160 | Time: ~60min

## What to Build

Convert ROS2 image, depth, and camera info messages into the internal RGB-D frame structure used by the tracker and mapper.

## Files

### Create: `src/anima_slam_coko/ros2/message_adapters.py`
```python
def ros_rgbd_to_frame(rgb_msg, depth_msg, info_msg) -> RuntimeFrame:
    ...
```

### Create: `tests/ros2/test_message_adapters.py`
```python
def test_ros_rgbd_to_frame_shapes():
    ...
```

## Verify

```bash
uv run pytest tests/ros2/test_message_adapters.py -v
```

## Done When

- [ ] adapter outputs `rgb`, `depth`, and `intrinsics`
- [ ] depth dtype is normalized to float
- [ ] intrinsics are extracted from `CameraInfo`
