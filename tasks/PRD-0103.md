# Task PRD-0103: Define Submap Schemas And Schema Tests

> PRD: PRD-01 | Depends: PRD-0102  
> Files: 3 | Lines: ~220 | Time: ~75min

## What to Build

Define typed submap records for rendered-depth and camera-depth modes, including Gaussian tensors, feature vectors, and pose history.

## Files

### Create: `src/anima_slam_coko/schemas/submap.py`
```python
class SubmapRecord(BaseModel):
    gaussian_xyz: NDArray[np.float32]
    keyframe_ids: NDArray[np.int64]
```

### Create: `tests/io/test_submap_schema.py`
```python
def test_rendered_depth_submap_roundtrip():
    ...
```

## Verify

```bash
uv run pytest tests/io/test_submap_schema.py -v
```

## Done When

- [ ] both runtime modes serialize and deserialize
- [ ] tensor shapes are asserted
- [ ] malformed payloads are rejected
