# Task PRD-0403: Add Bandwidth Audit And Paper Gap Report

> PRD: PRD-04 | Depends: PRD-0402  
> Files: 3 | Lines: ~220 | Time: ~75min

## What to Build

Add per-agent keyframe counts, total transmitted data accounting, and a paper gap report that compares local results to Tables 1-5.

## Files

### Create: `src/anima_slam_coko/eval/bandwidth.py`
```python
def summarize_transmitted_bytes(run_dir: Path) -> dict:
    ...
```

### Create: `tests/eval/test_bandwidth.py`
```python
def test_bandwidth_summary_contains_agents():
    ...
```

### Create: `artifacts/reports/paper_gap_report.md`
```markdown
# Paper Gap Report
```

## Verify

```bash
uv run pytest tests/eval/test_bandwidth.py -v
```

## Done When

- [ ] report includes rendering and communication gaps
- [ ] per-agent totals are captured
- [ ] reduction ratio is computed
